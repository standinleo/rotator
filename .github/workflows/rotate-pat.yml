name: Rotate GitHub PAT

on:
  workflow_dispatch:
    inputs:
      new_token_value:
        description: 'Paste your NEW GitHub PAT here'
        required: true
        type: string

jobs:
  rotate:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install libsodium
        run: npm install libsodium-wrappers

      - name: Save new token to file
        env:
          NEW_TOKEN: ${{ inputs.new_token_value }}
        run: |
          if [ -z "$NEW_TOKEN" ]; then
            echo "❌ No new token provided"
            exit 1
          fi
          echo -n "$NEW_TOKEN" > new_token.txt
          echo "✅ Token saved to file (length: ${#NEW_TOKEN})"

      - name: Get Repository Public Key
        id: get-key
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.ROTATION_TOKEN }}
          script: |
            const response = await github.rest.actions.getRepoPublicKey({
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            core.setOutput('key_id', response.data.key_id);
            core.setOutput('key', response.data.key);
            console.log('✅ Public key retrieved');

      - name: Encrypt New Token
        id: encrypt
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.ROTATION_TOKEN }}
          script: |
            const fs = require('fs');
            const sodium = require('libsodium-wrappers');
            await sodium.ready;

            const newToken = fs.readFileSync('new_token.txt', 'utf8').trim();
            if (!newToken) {
              throw new Error("New token is empty");
            }

            const key = Buffer.from('${{ steps.get-key.outputs.key }}', 'base64');
            const encryptedBytes = sodium.crypto_box_seal(newToken, key);
            const encrypted = Buffer.from(encryptedBytes).toString('base64');

            core.setOutput('encrypted_value', encrypted);
            console.log('✅ Token encrypted successfully');

      - name: Update Repository Secret
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.ROTATION_TOKEN }}
          script: |
            await github.rest.actions.createOrUpdateRepoSecret({
              owner: context.repo.owner,
              repo: context.repo.repo,
              secret_name: 'MY_DEPLOY_TOKEN',     # ← CHANGE to your actual secret name
              encrypted_value: '${{ steps.encrypt.outputs.encrypted_value }}',
              key_id: '${{ steps.get-key.outputs.key_id }}'
            });
            console.log('✅ Secret MY_DEPLOY_TOKEN updated successfully!');
